# iosenter.no code sample

A small TypeScript utility extracted from the prayer-times feature used in production at https://iosenter.no.

The function determines the next prayer based on the user's current local time.

The full iosenter.no repository is private. This public repository contains only a small, sanitized code sample.
